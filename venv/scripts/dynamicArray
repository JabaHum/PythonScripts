import sys
import os

# set n = 20

n = 50

data = []

for item in range(n):
    # number of elements
    a = len(data)
    # actual size of array in bytes
    b = sys.getsizeof(data)
    print(' Length of:{0:3d}; Size in Bytes:{1:4d}'.format(a, b))

    data.append(n)
